{
  "name": "brainoku-sudoku",
  "version": "1.0.0",
  "description": "Brainoku Sudoku — tägliches Hirntraining",
  "type": "module",
  "private": true,
  "scripts": {
    "build:levels": "node build-levels.mjs",
    "build:web": "node build-web.mjs",
    "cap:add:ios": "npm run build:web && cap add ios",
    "cap:sync": "npm run build:web && cap sync ios",
    "cap:open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/local-notifications": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
